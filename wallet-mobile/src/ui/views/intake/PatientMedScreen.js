import React from "react";
import { View } from "react-native";

import styles from "../../styles";

const PatientMedScreen = ({ navigation }) => {
  return <View></View>;
};
export default PatientMedScreen;
