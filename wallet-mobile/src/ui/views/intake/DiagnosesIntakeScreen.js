import { LinearGradient } from "expo-linear-gradient";

const DiagnosesIntakeScreen = ({ navigation }) => {
    return (
        <LinearGradient>
            <SafeAreaView>
                <View>
                    <Header />
                </View>
            </SafeAreaView>
        </LinearGradient>
    );
};