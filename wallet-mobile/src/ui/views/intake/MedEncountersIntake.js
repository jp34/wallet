import { View } from "react-native";
import Header from "../../components/Header";

const MedicationIntakeScreen = ({ navigation }) => {
    return(
        <View>
            <Header />
        </View>
    );
};

export default MedEncountersIntake;